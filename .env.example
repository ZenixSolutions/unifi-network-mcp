# Copy to .env for local development (never commit a real key).
UNIFI_CONSOLE_URL=https://192.168.1.1
UNIFI_API_KEY=replace-me
# Accept the console's self-signed certificate (warned on start; see SECURITY.md)
#UNIFI_INSECURE=1
